# Read Note

五线谱识别练习 SPA，技术栈：`React + Vite + TypeScript + TailwindCSS`。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## Deploy

```bash
npm run deploy
```

This command builds the app and force-pushes `dist` to the `gh-pages` branch.
